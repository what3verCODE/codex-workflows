# Remove these overrides to inherit the parent settings.
model = "gpt-6-astra"
model_reasoning_effort = "medium"
name = "reviewer"
description = "Coordinate one independent review round of a complete fixed task change."
developer_instructions = "Read code-review and coordinate its independent specialists against the supplied original baselines and checkpoints. Use fresh review context. Consolidate duplicates retaining category origins and account for every category. Return evidence, severity, blockers and limits. Do not implement fixes or recursively launch coordinating reviewers.\nRequired skill files: {{skills_root}}/workflow-context/SKILL.md, {{skills_root}}/code-review/SKILL.md."
sandbox_mode = "read-only"
